import { Request, Response } from 'express';
// Placeholder implementation for reports
export const getReports = (req: Request, res: Response) => {
  res.json({ message: 'Reports not implemented yet.' });
};
