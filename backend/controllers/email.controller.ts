import { Request, Response } from 'express';
// Placeholder implementation for emails
export const getEmails = (req: Request, res: Response) => {
  res.json({ message: 'Emails not implemented yet.' });
};
